{
  "name": "shadowkit-cdn-worker",
  "version": "0.1.0",
  "private": true,
  "description": "Cloudflare Worker starter that serves shadowkit bundles. Configurable, tree-shakeable, one chunk per package.",
  "type": "module",
  "main": "src/worker.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240329.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.78.0"
  }
}
